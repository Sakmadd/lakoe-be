export interface UpdateLocationDTO {
  id: string;
  location_id: string;
  name: string;
  address: string;
  city: string;
  district: string;
  postal_code: string;
  longitude: string;
  latitude: string;
  is_main: boolean;
}
